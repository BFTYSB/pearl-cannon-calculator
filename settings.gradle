pluginManagement {
	repositories {
		maven { url = uri('https://maven.fabricmc.net/') }
		maven { url = uri('https://maven.apache.org/snapshots/') }
		gradlePluginPortal()
	}
}

plugins {
	id 'org.gradle.toolchains.foojay-resolver-convention' version '0.10.0'
}

rootProject.name = archives_base_name
